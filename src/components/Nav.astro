<nav>
  <ul>
    <li><a href="#intro">🏠</a></li>
    <li><a href="#about">🙋</a></li>
    <li><a href="#work">💻</a></li>
    <li><a href="#travels">🛩️</a></li>
    <li><a href="#contact">📇</a></li>
  </ul>
</nav>

<style>
  nav {
    padding-block: var(--space-2);
  }

  ul {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-2);
    width: 100%;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  li {
    --animation-delay: 150ms;
    padding: var(--space-2);
    opacity: 0;
  }

  /* iterate over list items and fade in with more delay on each one */
  li:nth-child(1) {
    animation: fadeIn 0.5s var(--animation-delay) forwards;
  }

  li:nth-child(2) {
    animation: fadeIn 0.5s calc(var(--animation-delay) * 2) forwards;
  }

  li:nth-child(3) {
    animation: fadeIn 0.5s calc(var(--animation-delay) * 3) forwards;
  }

  li:nth-child(4) {
    animation: fadeIn 0.5s calc(var(--animation-delay) * 4) forwards;
  }

  li:nth-child(5) {
    animation: fadeIn 0.5s calc(var(--animation-delay) * 5) forwards;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }

  a {
    padding: var(--space-2);
    text-align: center;
    font-size: var(--size-5);
    filter: saturate(0);
    border-radius: var(--radius-2);

    &:hover {
      filter: saturate(1);
      background-color: var(--highlight);
    }
  }
</style>
